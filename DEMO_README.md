# 演示程序说明

## 功能描述
这个演示程序在LCD上显示一个频率变换的正弦波，频率从1Hz扫描到2Hz，再返回到1Hz，周期循环。

## 屏幕布局

```
┌─────────────────────────────────────────┐ 0
│      绿色标题栏 (20px高)                │
├─────────────────────────────────────────┤ 20
│                                         │
│          波形显示区域 (网格)             │
│         绿色绘制的正弦波动画             │
│                                         │
├─────────────────────────────────────────┤ 197
│  状态▮     频率显示    电压显示          │ 198-239 (紫色)
│                                         │
└─────────────────────────────────────────┘ 239
  0                                      319
```

- **波形显示区** (20-197)：320x177像素，显示正弦波
  - 绿色网格线（0.5V/格）
  - 绿色坐标轴
  - 绿色正弦波形

- **状态栏** (198-239)：320x42像素，紫色背景
  - **运行状态**：白色方块表示运行中
  - **频率显示**：使用7段数码管显示当前频率（1Hz-2Hz范围）
  - **电压显示**：使用7段数码管显示峰值电压

## 文件说明

### simple_font.c/h - 7段数码管显示
- `font_draw_digit()` - 绘制单个数字（0-9）
  - 使用7段LED显示原理
  - 可配置大小和颜色
  
- `font_draw_number()` - 绘制完整数字
  - 支持小数点、负号
  - 自动格式化显示

### demo_waveform.c/h - 波形生成
- `demo_generate_waveform()` - 生成演示正弦波
  - 参数说明：
    - `wave_buffer` - 输出波形数据缓冲区（320个样点）
    - `num_points` - 要生成的样点数
    - `start_freq` - 扫描起始频率（1Hz）
    - `end_freq` - 扫描结束频率（2Hz）
    - `frame_num` - 当前帧号（用于控制扫描进度）
  
  - 工作原理：
    - 每200帧循环一周
    - 前100帧：1Hz → 2Hz（线性扫描）
    - 后100帧：2Hz → 1Hz（线性扫描）
    - 采样率4000Hz，生成320个样点

### ui_oscilloscope.c/h - UI界面
- `ui_init_static_screen()` - 初始化UI界面（绘制网格和坐标轴）
- `ui_draw_waveform()` - 绘制波形（使用差分更新，只重绘变化区域）
- `ui_update_dynamic_info()` - 更新底部信息栏（频率、电压等）

### main.c
演示程序主循环：
1. 每次循环生成新的正弦波样点
2. 计算当前扫描频率（1→2→1）
3. 调用 `ui_draw_waveform()` 绘制波形
4. 调用 `ui_update_dynamic_info()` 更新频率和电压显示
5. 延迟50ms后循环

## 显示效果

- 屏幕顶部：绿色区域和坐标轴
- 中间：规则的网格背景，波形在网格上实时绘制和更新
- 底部：紫色状态栏
  - 左侧：运行指示（白色方块）
  - 中间：频率值（7段数码管显示，单位Hz或KHz）
  - 右侧：峰值电压（7段数码管显示，单位V）

## 编译和运行
确保添加了 `-lm` 编译标志（已在platformio.ini中配置），以链接数学库。

程序启动后将在LCD上显示：
- 网格背景（绿色网格线）
- 不断变化的正弦波（绿色）
- 底部信息栏显示当前频率和状态
- 频率平滑从1Hz扫描到2Hz，再返回1Hz，周期重复

